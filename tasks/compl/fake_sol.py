#import sympy as sp
z=input()#--
#z = sp.simplify(input())
#a = sp.re(z)
#b = sp.im(z)
#r = sp.sqrt(a ** 2 + b ** 2)
#if a == 0 or b == 0:
    #if b < 0:
       # arg = (3 * sp.pi) / 2
   # elif b > 0:
      #  arg = sp.pi / 2
   # elif a > 0:
       # arg = 0
#elif a < 0:
   # arg = sp.pi
#elif a > 0 and b > 0:
    #arg = sp.atan(b / a)
#elif a < 0 and b > 0:
    #arg = sp.pi - sp.atan(abs(b / a))
#elif a < 0 and b < 0:
    #arg = sp.pi + sp.atan(b / a)
#elif a > 0 and b < 0:
    #arg = 2 * sp.pi - sp.atan(abs(b / a))
#ans = str(r) + '(' + f'cos({arg}) + i sin({arg})' + ')'
print('a21412 + ;sa')
